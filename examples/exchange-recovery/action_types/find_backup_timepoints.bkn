---
type: action_type
id: find_backup_timepoints
name: 查找备份时间点副本
tags: [灾备, 备份查找, 时间点]
action_type: add
enabled: false
risk_level: low
requires_approval: false
---

## ActionType: 查找备份时间点副本

根据备份的exchange服务器查找该服务器的备份时间点副本。

### Bound Object

| Bound Object |
|--------------|
| exchange_server |

### Affect Object

| Affect Object |
|---------------|
| exchange_server |
| backup_timepoint |

### Pre-conditions

| Object | Check | Condition | Message |
|--------|-------|-----------|---------|
| exchange_server | property:status | != null | 服务器信息必须存在 |

### Scope of Impact

| Object | Impact Description |
|--------|---------------------|
| exchange_server | 仅查询服务器信息，不修改数据 |
| backup_timepoint | 仅查询时间点副本信息，不修改数据 |

### Tool Configuration

| Type | Toolbox ID | Tool ID |
|------|------------|---------|
| tool | recovery_toolbox | find_backup_timepoints |

### Parameter Binding

| Name | Type | Source | Binding | Description |
|-----------|------|--------|---------|-------------|
| server_name | string | property | server_name | Exchange服务器名称 |
| server_address | string | property | server_address | Exchange服务器地址 |

### Schedule

| Type | Expression |
|------|------------|
| EVENT | on_request |

### Execution Description

1. 接收Exchange服务器名称和地址作为输入参数
2. 在备份软件中查找该服务器的备份时间点副本
3. 按备份时间倒序排序
4. 输出时间点副本列表，包括：
   - 时间点副本ID、时间点名称、备份时间、时间点大小、是否验证
   - 每个时间点副本的元数据摘要
